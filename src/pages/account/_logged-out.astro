---
import CreateAccountBtn from "./_lib/create-account-btn";
import ImportAccountBtn from "./_lib/import-account-btn";
import AutoLogin from "./_lib/auto-login";
---

<div class="container mx-auto flex max-w-4xl flex-col gap-8 px-4 py-4 lg:py-12">
  <header>
    <h1 class="mb-6 text-4xl font-bold">
      <span class="mr-2 inline-block text-4xl">👋</span>
      Willkommen
    </h1>
    <p class="text-dimmed text-sm">
      Verwalte hier deinen Account und deine Anmeldedaten.
    </p>
  </header>

  <AutoLogin client:only />

  <section class="flex flex-col gap-4">
    <h2 class="text-2xl font-bold">Kein Account</h2>

    <div class="paper flex flex-col gap-4 p-6">
      <div class="flex items-center gap-2">
        <i class="ti ti-cloud-off text-gray-500"></i>
        <h3 class="text-lg font-bold">Nur lokal gespeichert</h3>
      </div>

      <p class="text-dimmed text-sm">
        Du arbeitest aktuell nur lokal. Erstelle einen Account oder melde dich
        an, um deine Daten zu synchronisieren und mit anderen
        zusammenzuarbeiten.
      </p>

      <div class="text-dimmed space-y-2 text-sm">
        <h4 class="font-bold">Deine Daten sind sicher</h4>
        <p>
          Wir speichern nur deinen selbst gewähnten Namen - keine persönlichen
          Daten. Alles bleibt privat und wird niemals weitergegeben.
        </p>
      </div>

      <div class="flex flex-row gap-2 pt-2">
        <CreateAccountBtn client:load />
        <ImportAccountBtn client:load />
      </div>
    </div>
  </section>
</div>
